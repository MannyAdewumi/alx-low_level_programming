#include <stdlib.h>
#include <stdio.h>
/**
 * div - function to divide two integers
 * @x: first integer
 * @y: second integer
 * Return: sum of integers
 */

int div(int x, int y)
{
	return (x / y);
}
