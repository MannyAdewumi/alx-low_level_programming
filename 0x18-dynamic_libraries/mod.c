#include <stdlib.h>
#include <stdio.h>
/**
 * mod - function to get remainder of two integers
 * @x: first integer
 * @y: second integer
 * Return: remainder of integers
 */

int add(int x, int y)
{
	return (x % y);
}
