#include <stdlib.h>
#include <stdio.h>
/**
 * mul - function to multiply two integers
 * @x: first integer
 * @y: second integer
 * Return: multiplied integers
 */

int mul(int x, int y)
{
	return (x * y);
}
