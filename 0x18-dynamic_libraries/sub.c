#include <stdlib.h>
#include <stdio.h>
/**
 * sub - function to subtract two integers
 * @x: first integer
 * @y: second integer
 * Return: sum of integers
 */

int sub(int x, int y)
{
	return (x - y);
}
